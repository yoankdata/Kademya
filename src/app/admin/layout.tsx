// src/app/admin/layout.tsx
import type { ReactNode } from 'react';

export default function AdminLayout({ children }: { children: ReactNode }) {
    // Pas de logique, pas de redirection, juste le rendu des enfants
    return children;
}
